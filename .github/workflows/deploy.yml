name: Deploy demo

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: https://example.com
    steps:
      - run: echo "deploy ok"
